# ERP 系统配置文件

server:
  # 运行环境: development, production
  env: development

  # 监听地址
  host: 0.0.0.0

  # 监听端口
  port: 8080

  # Gin运行模式: debug, release, test
  # 如果不设置，根据 env 自动选择: production -> release, 其他 -> debug
  mode: ""

log:
  # 日志级别: debug, info, warn, error, fatal
  level: info
